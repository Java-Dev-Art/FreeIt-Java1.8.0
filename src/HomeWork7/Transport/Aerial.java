package HomeWork7.Transport;

public class Aerial extends Transport{
    int wingspan;
    int minTakeOfPassLength;

    public Aerial(int power, int maxSpeed, String mark) {
        super(power, maxSpeed, mark);
    }
}
